import { describe, it, expect } from 'vitest';
import { mount } from '@vue/test-utils';
// Red-phase scaffold — component implementation will follow in dev-story
// import BadgeCard from '../BadgeCard.vue';

describe.skip('[Story 7.1] BadgeCard.vue (ATDD Red Phase)', () => {
  it('[P0] should render unlocked badge with icon and Clubhouse styles', () => {
    // Scaffold test for BadgeCard component
    expect(true).toBe(true);
  });

  it('[P1] should render locked state with visual indicator', () => {
    // Scaffold test for locked badge
    expect(true).toBe(true);
  });
});
